[project]
name = "uv-example"
version = "0.1.0"
description = "python-tips 레포의 uv 데모 프로젝트"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
]
